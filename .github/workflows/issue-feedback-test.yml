name: Issue feedback (supplied default configuration)

on:
  issues:
    types:
      - opened
      - reopened
      - edited

permissions:
  contents: read
  issues: write

jobs:
  feedback:
    uses: SecondSkoll/generic-agentic-workflows/.github/workflows/opencode-issue-feedback.yml@a38b4244024d6c458aef7d055693f9a145399394
    permissions:
      contents: read
      issues: write
    with:
      configuration_source: default
      configuration_ref: a38b4244024d6c458aef7d055693f9a145399394
      configuration_profile: issue-feedback
      focus: general
      max_issues: 20
    secrets:
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}