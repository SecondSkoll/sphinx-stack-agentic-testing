name: Release project review (self, local configuration)

on:
  release:
    types: [published]

permissions:
  contents: read
  issues: write

jobs:
  release-review:
    # The pin below must point to a reviewed release commit containing the
    # reusable workflow.
    uses: SecondSkoll/generic-agentic-workflows/.github/workflows/opencode-release-project-review.yml@67039ac7f3157d0986952a5cb2c087827f5a9e1f
    permissions:
      contents: read
      issues: write
    with:
      target_repository: ${{ github.repository }}
      release_id: ${{ github.event.release.id }}
      configuration_source: local
      configuration_profile: sphinx-stack-setup
      dry_run: true
    secrets:
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}